# Methodology

The canonical workflow maps the private questionnaire fields, parses duration, applies the 5th percentile duration screen, requires both attention checks, constructs derived variables, and runs OLS models with ordinary and HC3 robust standard errors. Mediation results use bootstrap confidence intervals as the primary inference. EFA is excluded from public main results because KMO, Bartlett, extraction, and rotation validation were not completed for this candidate package.
